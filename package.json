{
  "name": "docrack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -k -n REACT,ELECTRON,EXPRESS,PYTHON -c blue,green,yellow,magenta \"npm run dev --prefix frontend\" \"npm run start --prefix electron\" \"npm run dev --prefix express-backend\" \"cd python-backend && venv\\Scripts\\activate && python run.py\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
